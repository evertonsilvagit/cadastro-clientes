spring:
  datasource:
    url: jdbc:h2:mem:clientes
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
server:
  servlet:
      context-path: /cadastro/api/v1
  error:
    include-stacktrace: never
